# OpenAPI spec pin for code generation (independent of SDK semver).
# Update this file when cutting an SDK release that targets a new API contract.
#
# openapi.spec.ref: git tag, branch, or commit SHA on openapi.spec.repo
# openapi.api.version: must match info.version in the pinned openapi.yaml
# server.version.min: lowest Config Manager server version this SDK supports

openapi.spec.repo=arverma/config-manager
openapi.spec.ref=main
openapi.api.version=0.2.0
server.version.min=0.1.0
